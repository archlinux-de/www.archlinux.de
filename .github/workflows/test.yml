name: test

on:
  push:
  schedule:
    - cron: '0 1 */1 * *'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        type: ['test', 'test-db', 'test-e2e']
    steps:
      - uses: actions/checkout@v2
      - run: make install
      - run: make ${{ matrix.type }}
